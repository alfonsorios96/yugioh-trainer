# Optional fallback if Settings has no API key. The desktop app prefers the Settings UI.
# Use a model the API actually serves (e.g. gpt-4o-mini). Cursor-only names will not work.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
